node_modules/
dist/
server/data/cache/
*.log
.DS_Store
*.tsbuildinfo

# Per-machine Claude Code settings — the permission allowlist each checkout
# accumulates as it works. `.claude/settings.json` is the shared project
# config and stays tracked; this one is personal and conflicts on every merge.
.claude/settings.local.json

server/data/

# CDK synth output
infra/cdk.out/
infra/cdk.context.json

# Agent worktrees the harness creates under the repo — checkouts of this same
# repository, never content of it.
.claude/worktrees/
