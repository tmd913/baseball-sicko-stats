{
  "name": "statcast-sicko",
  "version": "1.0.0",
  "private": true,
  "description": "Visualize previous-day MLB batting data for a watchlist of players (Baseball Savant / Statcast).",
  "workspaces": [
    "server",
    "client",
    "infra"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "PORT=4000 npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "start": "npm run start --workspace server",
    "docs": "npx --yes serve docs -l 4001",
    "cdk": "npm run cdk --workspace infra --",
    "deploy": "npm run build && npm run cdk -- deploy"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
